# Airflow DAG runtime dependencies
# Astro CLI installs this file automatically into the Airflow Docker image.
# Keep minimal — only what dags/daily_data_refresh.py needs at runtime.
#
# Full ML/training stack is in requirements-ml.txt (local use only).

numpy<2
pandas
tpqoax
